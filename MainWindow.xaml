<Window x:Class="AutoSizeScan.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoSizeScan"
        mc:Ignorable="d"
        Title="AutoSize Scan" Height="600" Width="900" WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Scanner Selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Select Scanner:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="ScannerComboBox" Width="300" Height="30" HorizontalAlignment="Left"
                          DisplayMemberPath="DisplayName" SelectionChanged="ScannerComboBox_SelectionChanged">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="IsEnabled" Value="{Binding IsReachable}"/>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
                <Button x:Name="RefreshButton" Content="Refresh" Width="90" Height="30"
                        Margin="10,0,0,0" Click="RefreshButton_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="ScanButton" Content="Scan" Width="150" Height="40" 
                    Click="ScanButton_Click"/>
            <Button x:Name="SaveButton" Content="Save..." Width="150" Height="40" 
                    Click="SaveButton_Click" IsEnabled="False" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Preview Image -->
        <Border Grid.Row="2" BorderBrush="#DDD" BorderThickness="1" Background="#F5F5F5">
            <ScrollViewer x:Name="PreviewScrollViewer"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          PreviewMouseWheel="PreviewScrollViewer_PreviewMouseWheel">
                <Image x:Name="PreviewImage" Stretch="Uniform" Margin="10"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </ScrollViewer>
        </Border>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <TextBlock x:Name="StatusText" Text="Ready to scan" Foreground="#666"/>
            <TextBlock x:Name="DimensionsText" Text="" Margin="0,5,0,0" Foreground="#666"/>
        </StackPanel>
    </Grid>
</Window>
